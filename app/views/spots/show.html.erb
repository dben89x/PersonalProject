</div>
<div class = 'container jumbotron text-center'>

  <h1><%= link_to @spot.place.name, place_path(@spot.place) %></h1>
  <h2>at <%= @spot.start_time.strftime("%I:%M%p")  %> </h2>
  <h2>to <%= @spot.end_time.strftime("%I:%M%p")  %></h2><br>

  <h2> <%= @spot.description %> </h2><br>

  with...
  <h3>
    <% @spot.attendees.where(role: 'Attendant').each do |attendant| %>
      <%= link_to attendant.user.full_name, user_path(attendant.user) %> (Creator)<br>
    <% end %>

    <% @spot.attendees.where(role: 'Attendee').each do |attendee| %>
      <%= link_to attendee.user.full_name, user_path(attendee.user) %><br>
    <% end %>
  </h3>
</div>
<div class='container'>
  <%= link_to 'Edit', edit_spot_path(@spot) %>
  <%= link_to 'Delete', spot_path, method: :delete, data: {confirm: 'Are you sure?'} %>
</div>
